#Project properties
application.title=SistemPakarDiagnosaMotorMatik
application.vendor=Garasiku
application.main.class=sistempakar.Main
main.class=sistempakar.Main
javac.source=1.8
javac.target=1.8
javac.compilerargs=-Xlint:unchecked
javac.deprecation=false
javac.modulepath=
javac.classpath=
javac.processorpath=
javac.test.classpath=
javac.test.modulepath=
javac.test.processorpath=
build.dir=build
build.classes.dir=${build.dir}/classes
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
dist.dir=dist
dist.jar=${dist.dir}/SistemPakarDiagnosaMotorMatik.jar
dist.javadoc.dir=${dist.dir}/javadoc
dist.archive.exclude=
includes=**
excludes=
src.dir=src
test.src.dir=test
web.docBase.dir=${basedir}/web
user.properties.file=
debug.test.working.dir=
debug.working.dir=
run.jvmargs=
run.args=
run.classpath=${build.classes.dir}:${javac.classpath}
debug.classpath=${run.classpath}
test.classpath=${javac.test.classpath}:${build.test.classes.dir}
mkdist.disabled=false
browse.project=this
jar.compress=true
platform.active=JDK_1.8